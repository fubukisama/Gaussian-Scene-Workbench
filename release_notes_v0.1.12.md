# 3DGS Editor 0.1.12

- Add Experiment Manager for checkpoints, resume training, cloning experiments, parameter diffs, training curves, and job history.
- Add real GPU ID picking for visible-only Gaussian selection.
- Make floating panels resizable, scrollable, and persistent across sessions.
- Replace unsupported Electron prompt/confirm flows in Experiment Manager with inline controls.
- Add checkpoint mark, pin, delete, and TensorBoard/log curve parsing coverage.
- Keep desktop startup, environment, and job cancellation tests passing for the packaged app.
