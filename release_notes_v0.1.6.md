# 3DGS Editor v0.1.6

## Changes

- Fixes TrackballControls pan/zoom state leakage.
- Right-button vertical panning no longer consumes stale wheel or middle-button zoom deltas.
- Keeps existing left-button rotate, right-button pan, middle-button pan, and wheel zoom behavior.

## Notes

- Restart the editor after updating so the browser reloads the patched control module.
